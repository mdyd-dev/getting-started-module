---
layout_name: modules/getting-started/application
slug: installing-module
---
{% assign env = "https://qa2-portal.apps.near-me.com" %}
<p> Module is being installed... </p>

<script
        src="https://code.jquery.com/jquery-3.3.1.slim.min.js"
        integrity="sha256-3edrmyuQ0w65f8gfBsqowzjJe2iM6n0nKciPUp8y+7E="
        crossorigin="anonymous"></script>

<script type="text/javascript">
    var interval = null;

    $.urlParam = function (name) {
        var results = new RegExp('[\?&]' + name + '=([^&#]*)').exec(window.location.href);
        if (results == null) {
            return null;
        } else {
            return results[1] || 0;
        }
    };

    var checkIfReady = function () {
        var url = "{{ params.wait_for }}";
        fetch(url).then((response) => {
            if (response.status === 200) {
                document.location = url;
                if (interval) {
                    clearInterval(interval);
                }
            }
        }).catch((error) => { console.log('Not ready yet...')});
    };

    checkIfReady();
    interval = setInterval(checkIfReady, 3000);
</script>